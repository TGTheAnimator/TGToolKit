<UserControl x:Class="ToolKitV.Views.GlobalTranspilerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- Modern Scrollbar Style -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#33FFFFFF"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="{TemplateBinding Foreground}" CornerRadius="4"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT COLUMN: CONTROLS & OVERRIDES -->
        <DockPanel Grid.Column="0">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,25">
                <TextBlock Text="GLOBAL TRANSPILER" Foreground="#C0132A" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="12" Margin="0,0,0,5"/>
                <TextBlock Text="Server-Wide Automation Engine" Foreground="White" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="26" Margin="0,0,0,10"/>
                <TextBlock Text="Instantly modernize legacy scripts, sync locales, and enforce ecosystem standards across thousands of files simultaneously." Foreground="#999" FontFamily="Fonts/#TT Norms" FontSize="13" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Execution Button (Bottom) -->
            <Grid DockPanel.Dock="Bottom" Margin="0,20,0,0">
                <Border CornerRadius="8" Background="#C0132A">
                    <Button x:Name="RunGlobalButton" Content="RUN GLOBAL AUTOMATION" Height="50" Background="Transparent" Foreground="White" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand" Click="RunGlobalButton_Click"/>
                </Border>
            </Grid>

            <!-- Scrollable Settings -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Webhook Override -->
                    <Border Background="#111" BorderBrush="#222" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="🌐" Foreground="#C0132A" Margin="0,0,8,0" FontSize="16"/>
                                <TextBlock Text="GLOBAL WEBHOOK OVERRIDE" Foreground="White" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Replaces all 'Config.Webhook' values server-wide." Foreground="#666" FontFamily="Fonts/#TT Norms" FontSize="11" Margin="0,0,0,10"/>
                            <TextBox x:Name="WebhookInput" Background="#0A0A0A" Foreground="White" BorderBrush="#333" BorderThickness="1" Padding="10,8" FontFamily="Fonts/#TT Norms" FontSize="13" VerticalContentAlignment="Center" Height="35"/>
                        </StackPanel>
                    </Border>

                    <!-- Locale Override -->
                    <Border Background="#111" BorderBrush="#222" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,15">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="🌍" Foreground="#C0132A" Margin="0,0,8,0" FontSize="16"/>
                                <TextBlock Text="GLOBAL LOCALE OVERRIDE" Foreground="White" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Replaces all 'Config.Locale' values (e.g., 'en', 'fr')." Foreground="#666" FontFamily="Fonts/#TT Norms" FontSize="11" Margin="0,0,0,10"/>
                            <TextBox x:Name="LocaleInput" Background="#0A0A0A" Foreground="White" BorderBrush="#333" BorderThickness="1" Padding="10,8" FontFamily="Fonts/#TT Norms" FontSize="13" VerticalContentAlignment="Center" Height="35"/>
                        </StackPanel>
                    </Border>

                    <!-- Currency Override -->
                    <Border Background="#111" BorderBrush="#222" BorderThickness="1" CornerRadius="8" Padding="20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="💵" Foreground="#C0132A" Margin="0,0,8,0" FontSize="16"/>
                                <TextBlock Text="GLOBAL CURRENCY OVERRIDE" Foreground="White" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Replaces 'Config.Currency' and 'Config.CurrencyIcon'." Foreground="#666" FontFamily="Fonts/#TT Norms" FontSize="11" Margin="0,0,0,10"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Code (e.g. USD)" Foreground="#888" FontSize="10" Margin="0,0,0,5"/>
                                    <TextBox x:Name="CurrencyInput" Background="#0A0A0A" Foreground="White" BorderBrush="#333" BorderThickness="1" Padding="10,8" FontFamily="Fonts/#TT Norms" FontSize="13" VerticalContentAlignment="Center" Height="35"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Symbol (e.g. $)" Foreground="#888" FontSize="10" Margin="0,0,0,5"/>
                                    <TextBox x:Name="CurrencyIconInput" Background="#0A0A0A" Foreground="White" BorderBrush="#333" BorderThickness="1" Padding="10,8" FontFamily="Fonts/#TT Norms" FontSize="13" VerticalContentAlignment="Center" Height="35"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </DockPanel>

        <!-- RIGHT COLUMN: RECIPES & STATUS -->
        <Border Grid.Column="2" Background="#111" BorderBrush="#222" BorderThickness="1" CornerRadius="8" Padding="25">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="GOD-TIER TRANSPILER RECIPES" Foreground="White" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock Text="These recipes run against every file, automatically updating outdated logic to modern standards." Foreground="#888" FontFamily="Fonts/#TT Norms" FontSize="12" TextWrapping="Wrap"/>
                </StackPanel>

                <ItemsControl x:Name="RecipesList" Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#0A0A0A" BorderBrush="#333" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsActive}" VerticalAlignment="Center" Margin="0,0,15,0">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Recipe.RecipeId}" Foreground="#FFB347" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="13" Margin="0,0,0,3"/>
                                        <TextBlock Text="{Binding Recipe.Description}" Foreground="#AAA" FontFamily="Fonts/#TT Norms" FontSize="11" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Loading Overlay -->
                <Border x:Name="LoadingOverlay" Grid.Row="0" Grid.RowSpan="2" Background="#D0111111" CornerRadius="8" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="TRANSPILING" Foreground="#C0132A" FontFamily="Fonts/#TT Norms" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <ProgressBar x:Name="ScanProgress" Width="300" Height="8" Background="#222" BorderThickness="0" Margin="0,0,0,10">
                            <ProgressBar.Foreground>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#FF5555" Offset="0"/>
                                    <GradientStop Color="#C0132A" Offset="1"/>
                                </LinearGradientBrush>
                            </ProgressBar.Foreground>
                        </ProgressBar>
                        <TextBlock x:Name="StatusText" Text="Hunting for files..." Foreground="#AAA" FontFamily="Fonts/#TT Norms" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
